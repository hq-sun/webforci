import { DynamicallyCrudOptions, UseCrudProps } from "../d";
declare const defaultCrudOptions: {
    commonOptions(ctx?: UseCrudProps<any, any>): any;
    defaultOptions(opts: {
        t: any;
    }): DynamicallyCrudOptions<any>;
};
export default defaultCrudOptions;
