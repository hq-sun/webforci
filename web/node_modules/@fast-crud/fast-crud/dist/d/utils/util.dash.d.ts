import { Ref } from "vue";
declare const _default: {
    /**
     * 重构object，但忽略某些字段
     * @param ref
     * @param skips
     */
    omit(ref: Ref, ...skips: string[]): any;
};
export default _default;
